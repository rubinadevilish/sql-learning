-- Project: SQL Learning Journey
-- {DISTINCT remove duplicates. Don't use DISTINCT unless it's necessary; as it can slow down your query}

/* Return unique list of countries */
SELECT DISTINCT country
FROM customers